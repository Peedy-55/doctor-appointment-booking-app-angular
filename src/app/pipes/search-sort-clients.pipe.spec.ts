import { SearchSortClientsPipe } from './search-sort-clients.pipe';

describe('SearchSortClientsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSortClientsPipe();
    expect(pipe).toBeTruthy();
  });
});
