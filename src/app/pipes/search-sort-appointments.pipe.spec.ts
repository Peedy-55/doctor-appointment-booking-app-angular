import { SearchSortAppointmentsPipe } from './search-sort-appointments.pipe';

describe('SearchSortAppointmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSortAppointmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
