import { SearchAndSortDoctorPipe } from './search-sort-doctor.pipe';

describe('SearchDoctorPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAndSortDoctorPipe();
    expect(pipe).toBeTruthy();
  });
});
